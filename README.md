# Gedê App — Landing Page

Landing page estática hospedada no GitHub Pages.

## Deploy

Push para branch `main` — GitHub Pages serve automaticamente.

## Domínio próprio (futuro)

Criar arquivo `CNAME` na raiz com o domínio desejado (ex: `gede.app`).

## Assets

Salvos em `assets/`. Fonte: Figma file `5XHZosj0bV0kj0f4DCzYPx`, node `111:954`.
URLs do Figma expiram — se precisar atualizar, buscar via Figma MCP.
